name: Release

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore Crypto-Miner-Bitcoin-ETH.sln
      - name: Build
        run: dotnet build Crypto-Miner-Bitcoin-ETH.sln --configuration Release --no-restore
      - name: Test
        run: dotnet test Crypto-Miner-Bitcoin-ETH.sln --no-build --configuration Release
      - name: Publish
        run: dotnet publish src/Crypto-Miner-Bitcoin-ETH/Crypto-Miner-Bitcoin-ETH.csproj --configuration Release --no-build --output ./artifacts
